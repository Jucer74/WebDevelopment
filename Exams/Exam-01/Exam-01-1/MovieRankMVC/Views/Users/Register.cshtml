@model MovieRankMVC.Models.User

@{
    ViewData["Title"] = "Movie Rank";
}

<div class="container">
    <div class="row justify-content-center mt-5">
        <div class="col-md-6">
            <div class="card">
                <div class="card-body">
                    <div class="text-center mb-4">
                        <img id="profileImage" src="~/images/@(Model.SelectedProfileImage).png" alt="Profile Image" class="rounded-circle" style="width: 100px; height: 100px; object-fit: cover;" />
                    </div>
                    <h5 class="card-title text-center">@ViewData["Title"]</h5>
                    <hr class="my-4">
                    <form asp-action="Register" onsubmit="return validateForm()">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="mb-3">
                            <label asp-for="UserEmail" class="form-label">Email</label>
                            <input id="UserEmail" asp-for="UserEmail" class="form-control" />
                            <span asp-validation-for="UserEmail" class="text-danger"></span>
                            <span id="emailValidation" class="text-danger"></span> <!-- Custom validation message -->
                        </div>
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="FirstName" class="form-label">First Name</label>
                                <input id="FirstName" asp-for="FirstName" class="form-control" />
                                <span asp-validation-for="FirstName" class="text-danger"></span>
                                <span id="firstNameValidation" class="text-danger"></span> <!-- Custom validation message -->
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="LastName" class="form-label">Last Name</label>
                                <input id="LastName" asp-for="LastName" class="form-control" />
                                <span asp-validation-for="LastName" class="text-danger"></span>
                                <span id="lastNameValidation" class="text-danger"></span> <!-- Custom validation message -->
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="Password" class="form-label">Password</label>
                                <input id="Password" asp-for="Password" class="form-control" />
                                <span asp-validation-for="Password" class="text-danger"></span>
                                <span id="passwordValidation" class="text-danger"></span> <!-- Custom validation message -->
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="ConfirmPassword" class="form-label">Confirm Password</label>
                                <input id="ConfirmPassword" asp-for="ConfirmPassword" class="form-control" />
                                <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
                                <span id="confirmPasswordValidation" class="text-danger"></span> <!-- Custom validation message -->
                            </div>
                        </div>
                        <div class="mb-3">
                            <label asp-for="SelectedProfileImage" class="form-label">Select Profile Image</label>
                            <select id="SelectedProfileImage" asp-for="SelectedProfileImage" class="form-control" onchange="updateProfileImage()">
                                <option value="1">Profile picture 1</option>
                                <option value="2">Profile picture  2</option>
                                <option value="3">Profile picture  3</option>
                                <option value="4">Profile picture  4</option>
                            </select>
                        </div>
                        <div class="d-flex justify-content-between mt-3">
                            <button type="submit" class="btn btn-primary">Register</button>
                            <a asp-action="Login" class="btn btn-secondary">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    function updateProfileImage() {
        var selectedImage = document.getElementById('SelectedProfileImage').value;
        var profileImage = document.getElementById('profileImage');
        profileImage.src = `/images/${selectedImage}.png`;
    }

    function validateForm() {
        var userEmail = document.getElementById('UserEmail').value;
        var firstName = document.getElementById('FirstName').value;
        var lastName = document.getElementById('LastName').value;
        var password = document.getElementById('Password').value;
        var confirmPassword = document.getElementById('ConfirmPassword').value;

        // Clear previous validation messages
        document.getElementById('emailValidation').textContent = '';
        document.getElementById('firstNameValidation').textContent = '';
        document.getElementById('lastNameValidation').textContent = '';
        document.getElementById('passwordValidation').textContent = '';
        document.getElementById('confirmPasswordValidation').textContent = '';

        var isValid = true;

        if (!userEmail) {
            document.getElementById('emailValidation').textContent = 'Email is required';
            isValid = false;
        }

        if (!firstName) {
            document.getElementById('firstNameValidation').textContent = 'First Name is required';
            isValid = false;
        }

        if (!lastName) {
            document.getElementById('lastNameValidation').textContent = 'Last Name is required';
            isValid = false;
        }

        if (!password) {
            document.getElementById('passwordValidation').textContent = 'Password is required';
            isValid = false;
        }

        if (!confirmPassword) {
            document.getElementById('confirmPasswordValidation').textContent = 'Confirm Password is required';
            isValid = false;
        }

        if (password !== confirmPassword) {
            document.getElementById('confirmPasswordValidation').textContent = 'Passwords do not match';
            isValid = false;
        }

        return isValid;
    }
</script>
